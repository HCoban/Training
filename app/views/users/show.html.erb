<h2> <%= @user.username %> </h2>

<p>
  Welcome to your profile!
</p>

<h3><%= button_to "Create a New Sub", new_sub_url, method: :get %></h3>

<%= button_to "edit your profile", edit_user_path(@user.id), method: :get %>
<%= button_to "delete", user_path(@user.id), method: :delete, confirm: "Are you sure?" %>
