import { HotelConstants } from '../actions/hotel_actions';

const HotelReducer = (state = {}, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default HotelReducer;
